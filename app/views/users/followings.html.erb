 <h2 class="heading mt-3">Following</h2>

  <div class="row mt-5">

    <div class="col-md-1"></div>

    <div class="col-md-10">
      <table class='table table-hover table-inverse'>
        <thead>
          <tr>
            <th>images</th>
            <th>name</th>
            <th></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= link_to user_path(user) do %>
                <%= attachment_image_tag user, :image, :fill, 50, 50, fallback: "no-image-icon.jpg" %>
                <% end %>
              </td>
              <td><%= link_to user_path(user) do %><%=  user.name %><% end %></td>
              <td>フォロー数： </td>
              <td>フォロワー数： </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="col-md-1"></div>


  </div>