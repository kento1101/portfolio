<h2>Results index</h2>

<table class="table table-hover table-inverse">
　<% if @range == '2' %>
   <thead>
     <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
     </tr>
   </thead>
   <tbody>
      <% @style.each do |book| %>
       <tr>
        <td>
         <%= link_to(style.user) do %>
         <%= attachment_image_tag(style.user, :image, :fill, 50, 50, fallback: "no-image-mini.jpg") %>
         <% end %>
        </td>
        <td>
         <%= link_to style.name, style, class: "style_#{style.id}" %>
        </td>
        <td><%= style.detail %></td>
       </tr>
      <% end %>
   </tbody>
  <% else %>
   <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
     </tr>
   </thead>
   <tbody>
    <% @item.each do |item| %>
     <tr>
      <td><%= attachment_image_tag(item, :image, :fill, 50, 50, fallback: "no-image-mini.jpg") %></td>
      <td><%= user.name%></td>
      <td><%= link_to "Show", item, class: "item_#{item.id}" %></td>
     </tr>
    <% end %>
   </tbody>
   <% end %>
 </table>
