 <%if style.favorited_by?(current_user) %>
  <p>
    <%= link_to style_favorites_path(style), method: :delete, remote: true do %>
    ♥<%= style.favorites.count %>いいね
    <%  end %>
  </p>
  <% else %>
   <p>
    <%= link_to style_favorites_path(style), method: :post, remote: true do %>
    ♡<%= style.favorites.count %>いいね
    <%  end %>
  </p>
  <% end %>